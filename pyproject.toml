[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ghost"
version = "1.0.0"
description = "GHOST - Geospatial Heuristic Optimal Segment Tracker"
requires-python = ">=3.9"

[tool.vercel]
entrypoint = "app:app"
